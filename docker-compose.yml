version: '3.8'

services:
  ana-app:
    container_name: ana-platform
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3001:3000'
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    # Optional: Add volume for development
    # volumes:
    #   - .:/app
    #   - /app/node_modules
    #   - /app/.next
